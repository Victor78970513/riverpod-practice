import 'package:flutter/material.dart';

class PokemonInfoImage extends StatelessWidget {
  const PokemonInfoImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
