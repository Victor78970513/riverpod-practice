import 'package:flutter/material.dart';

class PokemonsByTypePage extends StatelessWidget {
  const PokemonsByTypePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello, World!'),
      ),
    );
  }
}
